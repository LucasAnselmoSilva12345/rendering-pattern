import '../title/style.css';

export function Title() {
  return (
    <div className="box-title">
      <h2>SSR vs SPA</h2>
      <span>Renderização pelo lado do servidor vs Renderização dinâmica</span>
    </div>
  );
}
